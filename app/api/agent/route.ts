import{askOpenAI}from"../../../lib/openai";import{createPcAgent}from"../../../lib/agent";import{recommend,Profile}from"../../../lib/recommender";import{refreshMarketPrices}from"../../../lib/market";
type Body={message:string;history?:{role:string;text:string}[];profile:Profile};
type Rec=Awaited<ReturnType<typeof refreshMarketPrices>>;
function fallback(profile:Profile,r:Rec){const fmt=(n:number)=>new Intl.NumberFormat("en-IN").format(n),parts=r.parts.map(x=>`• ${x.name} — ₹${fmt(x.price)} (${x.priceStatus==="live"?"live retailer check":"last verified snapshot"})\n  Product: ${x.url}`).join("\n");if(!r.parts.length)return`${r.title}. ${r.warnings[0]}`;return`I found a ${profile.kind} match within your ₹${fmt(profile.budget)} limit.\n\n${parts}\n\nEstimated total: ₹${fmt(r.total)}. ${r.warnings[0]} Prices marked as snapshots are not claimed as current; open the product link to confirm checkout price and stock.`}
function enforceLiveBudget(profile:Profile,rec:Rec):Rec{if(rec.total<=profile.budget)return rec;return{...rec,title:`No currently verified ${profile.use} ${profile.kind} within ₹${new Intl.NumberFormat("en-IN").format(profile.budget)}`,total:0,parts:[],checks:["Budget checked again after the retailer price refresh","No over-budget product was shown"],warnings:[`The selected product rose to ₹${new Intl.NumberFormat("en-IN").format(rec.total)}. Try a slightly higher budget or check again later.`]}}
function effectiveProfile(p:Profile,message:string):Profile{const text=message.toLowerCase();let next={...p};if(/\b(laptop|notebook)\b/.test(text))next.kind="laptop";else if(/\b(desktop|prebuilt)\b/.test(text))next.kind="desktop";else if(/\b(custom pc|pc build|build me|components?)\b/.test(text))next.kind="custom";if(/\b(gaming|games?|fps)\b/.test(text))next.use="gaming";else if(/\b(editing|design|creator|video|blender)\b/.test(text))next.use="creator";else if(/\b(coding|programming|developer|ai|machine learning)\b/.test(text))next.use="coding";else if(/\b(study|student|office|everyday|browsing)\b/.test(text))next.use="everyday";const m=text.match(/(?:₹|rs\.?\s*)?([0-9]+(?:\.[0-9]+)?)\s*(k|thousand|lakh|lac)?/i);if(!m)return next;let n=Number(m[1]);if(m[2]?.toLowerCase()==="k"||m[2]?.toLowerCase()==="thousand")n*=1000;if(m[2]?.toLowerCase()==="lakh"||m[2]?.toLowerCase()==="lac")n*=100000;if(n>=10000&&n<=250000)next.budget=Math.round(n);return next}
function localDeviceAnswer(message:string,profile:Profile,rec:Rec){const q=message.toLowerCase(),hello=/^(hi|hello|hey|good\s+(morning|afternoon|evening))\b/.test(q);if(hello)return"Hi! I’m Kavi 👋 I can help with laptops, custom PCs, desktops, upgrades, compatibility, and performance. Tell me what you use the device for and your budget—or ask any technical question.";if(/cpu.*gpu|gpu.*cpu|processor.*graphics/.test(q))return"The CPU handles general instructions, app logic, compilation, and game simulation. The GPU runs many calculations in parallel, so it matters most for games, 3D work, video effects, and local AI models. For coding, prioritize CPU and RAM; for gaming or local AI, reserve more of the budget for the GPU. Tell me your workload and I’ll suggest a sensible balance.";if(/ram|memory/.test(q))return"For everyday work, 16GB RAM is the practical minimum. Choose 32GB for large coding projects, virtual machines, 4K editing, or local AI tools. Match DDR4 or DDR5 to the motherboard—these generations are not interchangeable—and prefer two matched sticks for dual-channel performance.";if(/bottleneck/.test(q))return"A bottleneck means one component limits the others in a specific task. It is workload- and resolution-dependent, not a single universal percentage. Share your CPU, GPU, screen resolution, and main apps or games, and I’ll check the balance.";if(/compatib|will .* work|parts?/.test(q))return"Send the exact CPU, motherboard, RAM, GPU, power supply, case, and cooler names. I’ll check socket and chipset support, RAM generation, BIOS requirements, PSU capacity and connectors, plus GPU and cooler clearance.";if(/recommend|best|buy|under|budget|laptop|desktop|build/.test(q))return fallback(profile,rec);return"I can help with that device question. Please include the exact model or component names and what you want to know—performance, compatibility, upgrade options, value, or a buying recommendation. I’ll use the previous messages in this chat as context."}
export async function POST(request:Request){let body:Body|undefined;try{body=await request.json()as Body;if(typeof body.message!=="string"||body.message.length<2||body.message.length>2000)return Response.json({error:"Please send 2–2,000 characters."},{status:400});body.profile=effectiveProfile(body.profile,body.message);const rec=enforceLiveBudget(body.profile,await refreshMarketPrices(recommend(body.profile))),keys=process.env as{OPENAI_API_KEY?:string;GEMINI_API_KEY?:string},history=(body.history??[]).map(x=>`${x.role}: ${String(x.text).slice(0,600)}`).join("\n"),system=`You are Kavi, a warm, capable Indian device advisor. Greet the buyer naturally when they greet you. Answer any question about laptops, desktops, PC parts, performance, upgrades or compatibility, and use the conversation history for follow-ups. Ask one useful clarifying question when details are missing. For buying advice, the recommendation JSON is authoritative: never change its products, prices, URLs, total or budget result. Explain simply in under 220 words.\nProfile: ${JSON.stringify(body.profile)}\nRecommendation: ${JSON.stringify(rec)}\nConversation: ${history}\nBuyer: ${body.message}`;if(keys.OPENAI_API_KEY){const text=await askOpenAI(keys.OPENAI_API_KEY,system);return Response.json({text,recommendation:rec,marketCheckedAt:new Date().toISOString(),model:"openai"})}if(keys.GEMINI_API_KEY){const agent=createPcAgent(keys.GEMINI_API_KEY),result=await agent.generate({prompt:system}),sources=(result.sources??[]).map((s:any)=>({title:s.title||"Market source",url:s.url})).filter((s:any)=>s.url).slice(0,6),sourceText=sources.length?"\n\nSources:\n"+sources.map((s:any,i:number)=>`${i+1}. ${s.title}: ${s.url}`).join("\n"):"";return Response.json({text:result.text+sourceText,recommendation:rec,sources,marketCheckedAt:new Date().toISOString(),model:"gemini"})}return Response.json({text:localDeviceAnswer(body.message,body.profile,rec),recommendation:/recommend|best|buy|under|budget|laptop|desktop|build/i.test(body.message)?rec:null,model:"local-device-advisor"})}catch(error:any){console.error("agent_provider_failed",String(error?.message??error));if(body?.profile){const rec=enforceLiveBudget(body.profile,await refreshMarketPrices(recommend(body.profile)));return Response.json({text:localDeviceAnswer(body.message,body.profile,rec),recommendation:rec,model:"local-device-advisor"})}return Response.json({error:"I could not read that request. Please try again."},{status:400})}}



