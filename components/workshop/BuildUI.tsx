"use client";
import {ComponentKey} from "./Workshop3D";
const details:Record<ComponentKey,[string,string]>={cpu:["PROCESSOR","Executes game logic, code and creative workloads."],gpu:["GRAPHICS","Renders games, 3D scenes and accelerated AI tasks."],motherboard:["MAINBOARD","Connects every component and defines the upgrade path."],ram:["MEMORY","Holds active programs and data for fast access."],storage:["NVMe STORAGE","Stores Windows, games, apps and project files."],psu:["POWER SUPPLY","Delivers stable power with safe operating headroom."],cooling:["THERMALS","Moves heat away from the processor and case."],case:["CHASSIS","Defines airflow, clearance and motherboard support."]};
export function ComponentRail({selected,onSelect}:{selected:ComponentKey;onSelect:(x:ComponentKey)=>void}){return <aside className="component-rail"><span className="rail-label">BUILD MODULES</span>{Object.entries(details).map(([k,v],i)=><button key={k} className={selected===k?"active":""} onClick={()=>onSelect(k as ComponentKey)}><i>{String(i+1).padStart(2,"0")}</i><span><b>{v[0]}</b><small>{v[1]}</small></span></button>)}</aside>}
export function BuildHud({total,selected,compatible}:{total:number;selected:ComponentKey;compatible:boolean}){const metrics=[["TOTAL",total?new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(total):"AWAITING BUILD"],["FOCUS",details[selected][0]],["COMPATIBILITY",compatible?"PASSED":"CHECK REQUIRED"],["MARKET","INDIA · INR"]];return <div className="hud">{metrics.map(([a,b])=><div key={a}><small>{a}</small><b>{b}</b></div>)}</div>}
